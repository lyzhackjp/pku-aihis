# 贡献指南

## 教师与助教的最短路径

1. 用仓库的“课程任务”模板写明问题、对应周次、修改范围和验收条件。只附可公开的资料；私人输入用内部编号指代。
2. 指定一名任务负责人。涉及教案概念、史料或课堂安排，由教师作内容判断；涉及组件、构建或浏览器行为，由助教核验技术实现。
3. 在独立分支完成一项可评审的修改，填写 PR 模板，附页面预览或结果记录。
4. 审阅后合并，经过课堂检查再按发布流程上线。普通文档修改不要求重跑所有课件检查。

Issue 是任务卡，PR 是可以逐行比较的修改稿，Actions 是自动检查，Release 是一次正式版本的记录。

## 首次建立主线

核查时本地与远端都没有首次提交。此时没有可供 PR 比较的 main；首次初始化是常规分支流程的例外。

在用户布置首次入库任务后，先重新检查远端是否仍为空，再把本轮选定的规范文件明确加入一次初始化提交并推送 main。随后配置保护规则，再开始课件导入分支。如果远端已经出现提交，应先保全本地文件并对齐远端历史，不能另建一套无关历史或强制覆盖。

首次提交限规范和模板；助教工程的导入、原样复现、第三周内容修改分别留可追溯记录。首次治理提交已完成（04129ac），随后导入助教基础组件（524e075）；接续开发走下述分支和PR流程。

## 后续常规修改

以下为接续工作命令。进入仓库并确认工作区干净后：

```sh
git fetch origin
git switch main
git pull --ff-only
git switch -c docs/week03-workflow
```

根据任务选 `docs/`、`feat/`、`fix/`、`chore/` 前缀，如 `feat/week03-rag-demo`。分支名和 PR 只描述本次工作，不用 `final-final` 表达状态。

修改后先查看差异，再明确暂存相关文件。提交说明可用 `docs: 完善第三周来源核验要求` 这样的简短句式。推送、创建 PR 按当前任务授权执行；PR 采用 [.github/pull_request_template.md](.github/pull_request_template.md)。

一个 PR 可以包含同一任务必要的源码、说明、数据和截图；不要混入其他周次重构、依赖大升级或无关格式化。默认 Squash 合并，让一项任务形成一个可撤销的主线提交。

## 冲突和多人操作

- 同一工作目录同时编辑同一文件容易互相覆盖。确需同时开发时使用独立 checkout/worktree，并在任务卡写明责任文件；无需为普通单人修改引入额外工作树。
- 修改共享组件前列明受影响周次，与另一个修改者核对范围。
- PR 落后于 main 时，先更新 main 引用，在任务分支合并 `origin/main`，解决冲突后重跑受影响检查。不要为消除冲突覆盖别人的版本。
- 已共享分支原则上采用追加提交；确需重写历史时先协调。不要强推 main。

## 修改完成的标准

| 修改类型 | 最少应给出的证据 |
|---|---|
| 纯规范或说明 | 差异检查、引用及本地链接检查 |
| 史料、案例、讲解文字 | 来源定位、改动理由、内容审读结果 |
| 算法或数据 | 固定输入输出、参数、数据与模型版本、失败例 |
| 网页交互 | 构建结果、关键操作记录、截图、浏览器与尺寸 |
| 发布配置 | 受控预演、完整产物列表、子路径检查、恢复步骤 |

没有测试套件时，不把 `package.json` 中存在 `test` 命令写成测试通过。先判断已有检查是否适用于本次改动；必要时补能发现真实错误的检查。

教师与技术审阅均记录具体版本。AI 自检不能冒称他人独立审核，旧版审核也不能替代新版复核。

## 合并后的交接

任务卡记录已完成范围、未完成事项、PR 和版本；主线保持可构建。发布人填写 [发布记录](docs/templates/release-record.md)，注明成功部署的提交、时间和地址。若检查仅在本地完成，要写“本地验证”，不要写“已上线”。
