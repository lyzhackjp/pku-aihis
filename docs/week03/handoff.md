> 2026-09-23 更新：模型接入与实际生成已完成，以下09-22验收记录保留为历史。当前入口与范围见 [模型更新](model-update.md)。

# 第三周交接与验收

2026-09-22。教师已选混合交互：基础计算调参数、文本与结果并排、RAG与智能体分步展开。33页及讲解备注均在pages.json，教案对应见page-map.json。

## 输入与产物

本地识别19份日文PDF（NDLOCR-Lite，1,509页）及章太炎第8、14、10卷整卷（RapidOCR，1,560页），共3,069页、7,410个检索块。整卷包含非宗教材料，原件不改动。中文比较采用同样页面的Apple Vision与RapidOCR；3段286个汉字的人工对照中，字面编辑错误分别为7与5（2.45%与1.75%，含异体差异）。这是选择本次路线的有限抽样，不能当作全卷准确率。

公共入口使用27条短篇，明确区分已核正文与工作转录，附7张原页区域。日期字段分离言说/记录年、刊载年、整理版本；日记作者与现代整理者分列。两条教师论文引用关系有明确脚注页码。全库本地导入不自动上传。

## 已执行检查

- 3位独立subagent分别核对教案概念/排版、史料、工程。新教案13页，27组高亮补充，各有原论文或原库脚注，楷体左缩进实测0.74cm；图均在附录。AI检查不作为教师/助教审批。
- Node 24.19.0、pnpm 11.19.0、Stencil 4.45.0构建；5项有反例的数值检查与TypeScript通过。升级pnpm是在原助教10.10声明之外的明确适配，依赖锁文件保留。
- Chrome无头1440×900实际加载33页，未见页面异常或资源失败；Lucivy返回7条宗教命中，EdgeVec返回20条近邻。完整ZT8整卷导入后Lucivy实际建库检索成功；测得约2.65秒仅反映本机与此次输入，不作性能承诺。
- OpenCC转换、正则Worker、输入框方向键、聚焦按钮空格、D12初始演讲者页与下一页同步、RAG输入变化使旧请求失效、CLIP查询切换、48图块近邻、390px无整页横向溢出均通过。
- 非预设问题“佛學宗教”已在真实浏览器完成MiniLM编码和向量检索，测试使用固定修订的本机模型副本，未把结果冒充在线下载验证。ONNX推理运行文件随站点提供；模型权重仍需首次联网下载。延迟生成响应的模拟测试确认重新开始后不会显示旧答案。
- Tantivy、Chroma、Qdrant和Milvus Lite采用最终公共语料重跑，结果随版本和时间写在tools.json。WASM不是服务器版数据库的完整替代。

## 仍需课堂配置的外部条件

生成模型未配置，因此RAG答案与模型工具循环尚无真实模型运行记录；实现含明确错误显示、4轮预算、读ID校验及过期请求丢弃。AnythingLLM、Elasticsearch、OpenSearch、PaSa、PaperQA2、deep-research和评测服务提供单独本地部署方案，未声称已运行。D30本次使用OCR与人工描述，未调用视觉语言模型。D31使用真实开源CLIP共同空间，替代需要服务密钥的阿里接口。D32保留助教同款Shikiji模型，COS整库本次不可访问，改从其训练源清单中的同一公开数据源取得48图块，未声称独立测试准确率。

实际投影设备、Safari及教师模型账户尚未在本轮测试。课堂备用从本地静态包启动；首访GitHub Pages仍需网络，service worker只加响应头，不是离线缓存。

## 助教从哪里改

- 页面内容/交互：apps/week03/src/components/lesson-lab；导航和双屏：deck-container、deck-presenter。
- 讲解备注与对应：src/index.html、assets/data/pages.json、docs/week03/page-map.json；三者修改时一起核对。
- 史料：assets/data/corpus.json；任何正文改动后按local/PRECOMPUTE.md重算向量和本地检索结果。完整资料由教师本地提供，不从公开Git下载。
- 新模型：先核配置和误差；浏览器自由编码仅接受已验证MiniLM固定修订，其余向量包只用配套查询。
- 生成服务：local/runtime.py读环境变量，浏览器无密钥存储。
- 发布：见docs/ci-and-release.md；不要把单周包覆盖全站。协作者账号待教师指定，不擅自邀请。
