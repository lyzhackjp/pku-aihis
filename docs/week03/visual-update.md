# 2026-09-23 直观演示、应用联动与口语稿

33页演示新增问题、步骤箭头、当前状态和观察说明。D02字符变化、D03切块区间、D04词项格子、D07公式/饱和曲线、D08余弦/点积/L2几何、D09同刻度融合贡献、D10位置折扣、D12近似/精确对照、D21反馈方向、D24真实事件、D30—32分值含义与原图对照均已落到交互。没有内部访问记录的引擎不伪造执行动画。

D06提供六种可复制、随机抽取的可解释规则、捕获组和副本替换。D05的神经稀疏教学构造已经替换为官方多语模型实际输出：27条文档、9个查询、固定模型修订、原文哈希与运行参数。剪枝改变非零词项，共同词项点积随之重算；与Word2vec任务示意和真实MiniLM稠密表示分别标明。

## 独立助教复核

按教师要求，完成第一轮后由独立助教代理参照助教源码与课程制作skill审查并修改，详见[完整报告](ta-visual-review.md)。随后由主任务完成实际应用安装和联动，两个阶段的验收范围分别记录。

## 应用和模型

[部署与边界](local-applications.md)列明真实服务、库模式和原项目适配。新增固定Qwen3-4B-Instruct-2507 Q4_K_M用于实时回答，新增Qwen3 Embedding 0.6B用于应用。旧Qwen3别名此次是Thinking-2507，旧输出没有重新标记为Instruct。模型摘要、参数量和版本写入local-models.json；模型检测排除不能生成答案的embedding模型。

AnythingLLM有真实运行截图、来源与网页重跑；Elasticsearch和OpenSearch为独立单节点。PaSa采用原调度＋本地模型和语料适配，未宣称原CUDA两套7B环境复现；PaperQA运行Docs证据处理链；研究评测为自拟单题，不宣称官方全量榜单。模型真实错误、材料不足和无候选保留，页面附核查说明。

## 验收

- 12项浏览器端数值/接口测试，5项Python端点测试，类型检查与生产构建通过。
- Chrome 1440×900及390×844遍历全部33页，所有步骤按钮检查通过，无页面异常与横向溢出。
- 关键交互实际验证：正则匹配、BM25参考曲线、RRF共同刻度、HNSW参数约束、Rocchio范围与待应用状态、工具候选卡、多模态固定刻度。
- 六种检索实现从课堂桥接实跑，均返回五条候选。AnythingLLM、PaperQA和研究适配另经实际联动检查。
- D24本机4B模型实际执行search/search/read/finish提案，最终回答未满足已读引用要求，被执行器拒绝；事件时间线准确显示失败，未冒称成功答案。
- 公开站点白名单检查通过：模型权重、完整OCR、教师教案、口语稿、本机密钥和路径不发布。

## 教师文稿与协作

在教师工作区新增约2.4万字口语稿，覆盖D00—D32；宋体正文、楷体缩进操作提示，含33个演示链接。Typst与16页PDF均保存在“直观演示与口语稿_20260923”，逐页渲染验收，原教案不覆盖。

助教接续先看本报告和逐页设计，再从最新main建立短分支。新增演示须区分机制示意、保存结果和当前运行；改正文后核对向量与稀疏结果的哈希，改参数后检查显示是否同步。提交PR后通过build-week03，Squash合入，再手动部署Pages。私人应用环境与模型不进入Git。
