# 工具分类简化预览

本分支 `feat/week03-tool-families` 按教师2026-09-24的要求制作。main保留33页详细版与已合并的语料追加修复；这里的29页版本需教师看过效果后再决定合并，不发布覆盖主线课堂。

## 课堂内容的变化

| 详细版页面 | 简化版页面 | 课堂重点 |
| --- | --- | --- |
| D14 AnythingLLM | D14 知识库产品 | 材料、检索、回答如何组成工作区；检查一次回答的依据 |
| D15 Elasticsearch、D16 OpenSearch、D17 Tantivy | D15 搜索引擎 | 保持同一查询，切换代表工具，阅读候选与出处 |
| D18 Chroma、D19 Qdrant、D20 Milvus Lite | D18 向量数据库 | 保存兼容向量、寻找相近片段、回读原文 |

其余页面内容和编号保留。D15的下一页为D18，随后为D21；旧D16/D17链接转到D15，旧D19/D20链接转到D18。课程地图、教师备注与双屏翻页同步更新。三类按本课用途划分，不宣称软件能力互斥。

每页按“这一类解决什么 → 看一次实际例子 → 课后怎样探索”展开。课堂只抓一个问题，示例默认收起；切换工具后，学生看到实际结果的前三条候选，可打开完整节录、定位、核对状态与原页图像。安装、接口和配置不再作为七页必讲步骤，保留在指南及可展开的完整记录中。课后任选一个代表工具，加入不同类别资料，记录一次有用命中和一次漏检。

保留七个应用原有的实跑记录、版本与模型标识。主按钮明确为“查看保存示例”；“已安装：在本机重跑”单独展开。调用失败清空本次输出，教师可主动切回保存示例。向量库的本机轻量示例选择已编码查询，不把任意未编码问题伪装成真实查询。

保存示例来自应用原有的27条课堂库；顶部“语料库管理”追加的内容只进入浏览器库，不会自动重建外部应用索引。D07—D13可以检索浏览器中新追加的多类语料。

## 预览与复核

教师本机使用课程目录中《协作修改与简化预览_20260924》的“打开简化版.command”。比较入口为详细版8771、简化版8772，避免切换main的工作区或覆盖线上版本。

其他协作者按README完成依赖安装、构建并运行 `python3 local/runtime.py --port 8772`，打开 `http://127.0.0.1:8772/week03/#D14`。保存示例不要求模型服务；实际应用重跑另按本地运行指南准备环境。

2026-09-24验证：27项Node测试、9项Python测试及5项语料内容复核测试通过；类型检查、正式构建、29页与两份页面地图一致性、公开包与来源资产检查通过。公开跨领域语料打包与2686条导入校验通过。

Chrome检查29页在1440×900和390×844的显示；七种工具的保存结果及完整原文核对、指南链接、窄屏滚动、旧编号跳转、双屏教师备注与翻页均通过，无页面异常。实际调用Chroma完成“宗教”查询；另外以明确的受控503响应检查失败清屏及保存示例回退。此次没有重新运行生成模型，也没有声称七个外部应用全部重跑。浏览器脚本为 `scripts/check-week03-families-browser.mjs`；本机截图与结果保留在课程目录，不提交私人路径。

本分支保留追加导入逻辑。main上的追加修复另见 [追加导入说明](append-import.md)，其线上版本也经过追加、去重、向量补齐、整批冲突回退和保留原有查询的浏览器检查。
