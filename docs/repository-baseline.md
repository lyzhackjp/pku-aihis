> 本页保存2026-09-21初始核查，以下“未实施”均指当时状态。当前实现见README、docs/week03/handoff.md与ci-and-release.md。

# 仓库与输入工程基线

核查日期：2026-09-21。本记录区分实际观察与待实施事项。

## 仓库状态

| 项目 | 本轮实际结果 |
|---|---|
| 远端 | `https://github.com/lyzhackjp/pku-aihis.git` |
| 本地初始状态 | 目录仅含 `.git/`，main 尚无首次提交，无课件文件 |
| GitHub 元数据 | 仓库公开，默认分支名称 main，size 为 0 |
| GitHub 分支查询 | 返回空列表，与本地无首次提交的状态相符 |
| 远端管理能力 | 当前 GitHub 连接器返回 admin 权限；本轮仅只读查询 |
| 本轮新增 | README、AI 工作规则、贡献指南、流程文档、任务与 PR 模板、忽略规则 |
| 提交、推送、PR、线上设置 | 均未执行 |

“main 是默认分支名”不代表该分支已经有提交。首次入库前应重新核查，避免与教师或助教随后初始化的历史冲突。

## 输入工程

| 输入 | 已核查内容 | 尚未核查 |
|---|---|---|
| `slides-src.zip` | Stencil 与 TypeScript 工程；pnpm 锁文件；19 个组件的 TSX 文件；存在 `build`、`start`、`test`、`build:dsh-preview` 脚本声明 | 依赖安装、编译、浏览器运行、预置 dsh 资源的完整重建 |
| `guizang-ppt-skill.zip` | skill 主文档、脚本、Stencil 模板、旧 HTML 模板、参考文件及 LICENSE | 课件制作与模板实际运行 |

两个 ZIP 是不同用途：`slides-src.zip` 包含助教已修改的演示源码；skill ZIP 提供方法、脚手架及资源。后续复用时应以源工程为基线，不用通用脚手架覆盖它。

ZIP 的 SHA-256：

```text
slides-src.zip
f9111a05b69f367b567496990cf4ae7293f7cfac72af265b0839a82b05b67b1c

guizang-ppt-skill.zip
fbe9df74ba0e47534426b958e132fdde3f8e0e946d324e8527126d42a4891ba1
```

`slides-src.zip` 与交接包所附 `slides-src(1).zip` 的散列一致。19 个组件文件的存在不表示19个组件都被入口使用，更不表示它们全部通过测试。

skill 模板与演示工程的 `stencil.config.ts` 已有差异：复制 assets 时，模板使用 `keepDirStructure: true`，演示工程使用 `false`。后续应对照实际输出核查路径，不能把两份配置直接覆盖合并。

skill 包含 AGPL-3.0 许可证文本；演示工程 ZIP 根目录未见 LICENSE。后续导入保留已有声明和来源记录，分别核对代码、字体、图像、史料及课程文字的使用范围；本轮没有为整个仓库另行指定许可证。

## 状态解释

- 本轮核对了交接说明、第三周问题反馈、最新版修改方案、现有教案及演示方案，并核验交接清单；详细本地续接路径保存在课程文件夹。
- 第三周完整正文仍为 v2.0；`proposal_v3` 仍是修改方案。没有生成新的教案正文、课件或模型数据。
- 交接中的软件性能、候选检索组件和案例待核项沿用“待核／待测”，本轮没有重新验证其技术或史学结论。
- 本轮命令行的远端读取未能连接沙箱内配置的本机代理；GitHub 连接器读取成功。未据此修改用户代理或凭据，也没有把命令行通道记为已通过推拉测试。
- 当前 shell 没有解析到 `node`；找到了 `pnpm` 路径和 GitHub CLI。仅找到可执行文件不能证明环境、登录或构建成功。

本轮的通过结论限于资料接续、skill 文件安装和规范文件检查。软件复现、PR 自动检查及 Pages 部署留待工程导入阶段。
