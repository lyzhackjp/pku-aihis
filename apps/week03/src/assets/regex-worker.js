self.onmessage=e=>{try{const{text,pattern,replacement}=e.data;if(!pattern)throw Error('请输入模式');const r=new RegExp(pattern,'gu');const matches=[];for(const m of text.matchAll(r)){matches.push({text:m[0],index:m.index,captures:m.slice(1)});if(matches.length>=300)break;}self.postMessage({engine:'JavaScript /gu',matches,truncated:matches.length===300,replacement_preview:text.replace(r,replacement)});}catch(err){self.postMessage({error:String(err)});}};
