# AnythingLLM｜D14

本说明对应 2026-09-23 的实际安装与运行。教师本机的应用数据、源代码、模型与日志位于“第三周（9月24日）/本地应用与服务”及本周既有本地模型目录。点击“启动完整课堂.command”，保持窗口开启；进入 http://127.0.0.1:8767/week03/ 。公开网页只发布课堂片段、脱敏运行结果和截图，不包含密钥、模型权重或完整史料。

“本地重跑”调用真实服务，失败时保留错误；不会用保存结果冒充本轮响应。页面的步骤按钮只切换解释。外部API接入在“模型接入”面板中设置，适用于独立模型实验；各应用使用其自身配置的本地模型。

## 当前部署

已安装 AnythingLLM 1.16.2，将导入、切分、嵌入、向量检索和回答组织成工作区。使用 Qwen3 4B Instruct 和 Qwen3 Embedding 0.6B。

应用、模型和语料均在本机。网页展示保存结果，也可通过课堂桥接实际重跑。浏览器访问本地网络可能需要系统授权。

实际运行时间：2026-09-23T11:17:33+0800。版本：1.16.2。查询：井上哲次郎如何区分神道与宗教？请根据史料给出两点说明，并注明片段编号和PDF页序。。

## 建立工作区

打开本机 AnythingLLM，进入“第三周史料检索课堂”。27个带出处片段已导入。

```text
生成：qwen3:4b-instruct-2507-q4_K_M
嵌入：qwen3-embedding:0.6b
应用：AnythingLLM 1.16.2
本机入口：http://127.0.0.1:3301
```

观察：界面、模型和数据服务不是同一层。

## 导入与处理

导入准备好的小型带出处文本，观察切分和向量写入；完整PDF可在本机导入。

```text
记录：source_id / PDF页序 / 文章标题
设置：chunk size / overlap / embedding provider
```

观察：切块有没有丢掉页码或主体？模型改变后是否重建？

## 问答与引用

对同一问题查看检索到的上下文，再逐条回查回答引用。

```text
问题：哪些段落讨论宗教与道德的关系？
对照：删除关键文档后重新提问。
```

观察：回答是否随着可用证据改变？引用是否真支持论断？

## 实跑核查

保存回答是模型对材料的判断，教师仍须检查。截图中的 <tool_call> 是本次模型重录旧字时产生的异常，不是史料原文；请对照右侧原始片段。

## 原项目

[AnythingLLM 原代码库](https://github.com/Mintplex-Labs/anything-llm)
