import test from 'node:test';import assert from 'node:assert/strict';import {bm25,cosine,rrf,metrics,chunks,validateCorpus,parseStopwords,filterStopwords,tfidfForTerm} from '../src/lib/math.ts';
test('字面检索不为零命中制造结果；参数与长度可解释',()=>{const d=[{id:'a',text:'宗教 宗教'},{id:'b',text:'宗教 国家 道德'}];assert.equal(bm25(d,'不存在').length,0);const r=bm25(d,'宗教',{mode:'word'});assert.equal(r[0].id,'a');assert.ok(r[0].parts[0].idf>0);});
test('余弦约束与零向量',()=>{assert.equal(cosine([1,0],[0,1]),0);assert.equal(cosine([2,0],[1,0]),1);assert.throws(()=>cosine([0,0],[1,2]));assert.throws(()=>cosine([1],[1,2]));});
test('RRF缺席不贡献且使用名次',()=>{const r=rrf([[{id:'a'},{id:'b'}],[{id:'b'}]],60);assert.equal(r[0].id,'b');assert.ok(Math.abs(r[0].score-1/62-1/61)<1e-12);});
test('nDCG理想排序包含检索之外的高相关项',()=>{const r=metrics(['a'],{a:1,b:3},1);assert.equal(r.ndcg,1/7);assert.equal(r.precision,1);assert.equal(r.recall,null);assert.equal(metrics(['z'],{a:1},1).unjudged,1);});
test('分块覆盖与输入检查',()=>{assert.deepEqual(chunks('123456',4,2).map(x=>x.text),['1234','3456']);assert.throws(()=>chunks('abc',2,2));assert.throws(()=>validateCorpus([{id:'a',text:'a'},{id:'a',text:'b'}]));});
test('停用词解析与过滤真实生效',()=>{const s=parseStopwords('の、に を\nの　は');assert.deepEqual([...s].sort(),['に','の','は','を']);assert.ok(parseStopwords('Ａｂ').has('ab'));const r=filterStopwords(['の','宗教','は','宗教'],s);assert.deepEqual(r.kept,['宗教','宗教']);assert.deepEqual(r.removed,['の','は']);assert.deepEqual(filterStopwords(['の'],s,false).kept,['の']);assert.deepEqual(filterStopwords(['の'],new Set()).removed,[]);});
test('TF-IDF以全部当前语料计算分母与每条得分',()=>{const docs=[{id:'a',title:'甲'},{id:'b',title:'乙'},{id:'c',title:'丙'},{id:'d',title:'丁'}],bags=[{宗教:2},{宗教:1},{国家:1},{学问:1}];const r=tfidfForTerm(docs,bags,'宗教');assert.equal(r.n,4);assert.equal(r.df,2);assert.ok(Math.abs(r.idf-(Math.log(5/3)+1))<1e-12);assert.deepEqual(r.rows.map(x=>x.id),['a','b']);assert.ok(Math.abs(r.rows[0].value-2*r.idf)<1e-12);assert.notEqual(r.idf,Math.log(4/3)+1);});
